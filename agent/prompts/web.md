---
description: Search the web with Brave + Serper fusion and return sourced results
---
Search the web for: $ARGUMENTS

Instructions:
- If no query was provided, ask one concise clarifying question.
- Use the `web_search` tool with:
  - `query`: "$ARGUMENTS"
  - `backend`: "all" (unless user requested a specific backend)
  - `numResults`: 8-10 by default
- Then provide:
  1. A concise summary
  2. Key findings
  3. Source links
